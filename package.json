{
  "name": "yom",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "check": "tsc --noEmit",
    "format": "prettier --check \"index.html\" \"src/**/*.{ts,css}\" \"tests/**/*.ts\""
  },
  "devDependencies": {
    "@types/node": "^24.3.0",
    "prettier": "^3.6.0",
    "typescript": "^5.9.2",
    "vite": "^7.1.3",
    "vitest": "^3.2.4"
  }
}
