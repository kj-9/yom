# yom タスク一覧

## 直近

- [x] `README.md` を `~/work/repos/yom` 前提の手順に更新する
- [x] `.gitignore` を追加して `.venv/` `__pycache__/` などを除外する
- [x] 初回コミットを作成する
- [ ] 必要なら GitHub リモートを追加する

## 品質

- [x] `pytest` を導入して基本テストを追加する
- [x] Markdown ツリー生成のテストを書く
- [x] パス解決とディレクトリ外参照拒否のテストを書く
- [x] 更新監視の挙動を確認するテストまたは検証手順を整える
- [x] `ruff` か `python -m compileall` ベースの最低限の静的チェックを入れる

## 機能改善

- [x] ディレクトリの折りたたみ UI を追加する
- [ ] 現在表示中ページの自動スクロール追従を改善する
- [ ] トップページの既定表示ファイル選択ロジックを見直す
- [ ] Markdown 拡張機能の設定を CLI から切り替えられるようにする
- [ ] 画像やローカルリンクの相対パス解決を改善する
- [ ] CSS と HTML テンプレートを Python から分離する

## watch / サーバー

- [ ] `watchdog` 対応を追加してポーリング監視を置き換え可能にする
- [ ] ファイル更新時に現在表示中ドキュメントだけ差分更新する方式を検討する
- [x] ポート使用中のときのエラーメッセージを改善する
- [x] ブラウザ自動起動オプションの要否を判断する

## CLI

- [x] 起動時はデフォルトでブラウザを開く
- [x] `--no-open` を追加する
- [x] `--no-watch` を追加する
- [x] `--title` を追加する
- [x] `yom --help` の説明文を詰める
- [x] `pyproject.toml` に開発用 dependency group を追加する

## 配布

- [ ] ライセンスを決めて追加する
- [ ] バージョン管理方針を決める
- [ ] PyPI 配布の要否を判断する
